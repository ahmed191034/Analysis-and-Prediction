# Analysis and Prediction

## Status

⚠️ The repository currently contains only `Project.zip` — no visible notebook, script, or README content. GitHub can't render what's inside a zip, so as it stands this repo shows nothing to a visitor.

## What To Do

1. Extract the zip and commit the actual notebook/script and dataset (or a data-download step) directly to the repo.
2. Add a proper README describing the dataset, the business/analysis question being answered, the modeling approach, and key findings.
3. Rename the repo itself to something more specific once the content is known (`Analysis-and-Prediction` is generic).
